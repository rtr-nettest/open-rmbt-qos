<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Console is always on -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- LOG_HOST / LOG_PORT are read natively from env vars (or system properties), with
         defaults, so resolution does not depend on Spring (this is a plain, non-Spring app).
         An unset LOG_HOST resolves to an empty logHost rather than the "..._IS_UNDEFINED" token. -->
    <property name="logHost" value="${LOG_HOST:-}"/>
    <property name="logPort" value="${LOG_PORT:-4560}"/>

    <!-- Register the Logstash appender only when LOG_HOST is set; otherwise fall back to
         console-only so a missing destination can't crash startup. -->
    <if condition='!property("logHost").isEmpty()'>
        <then>
            <appender name="logstash" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
                <param name="Encoding" value="UTF-8"/>
                <destination>${logHost}:${logPort}</destination>
                <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                    <customFields>{"app_name":"qos-service"}</customFields>
                </encoder>
            </appender>

            <root level="INFO">
                <appender-ref ref="CONSOLE"/>
                <appender-ref ref="logstash"/>
            </root>
        </then>
        <else>
            <root level="INFO">
                <appender-ref ref="CONSOLE"/>
            </root>
        </else>
    </if>
</configuration>
